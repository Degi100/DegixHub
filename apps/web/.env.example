# API Backend URL (used by API Routes proxy on server-side)
# Development: http://localhost:3003
# Production: http://localhost:3003 (internal Docker network)
API_URL=http://localhost:3003

# Public API URL (only used for direct client-side calls in development)
NEXT_PUBLIC_API_URL=http://localhost:3003/trpc

# Environment
NODE_ENV=development
